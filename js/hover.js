      <!--Pasha-->
	  $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#trigger').hover(function(e) {
          $('div#pop-up').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#pop-up').hide();
        });
        
        $('a#trigger').mousemove(function(e) {
          $("div#pop-up").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Pasha-->

      <!--Drew-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#drew-hover').hover(function(e) {
          $('div#drew-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#drew-box').hide();
        });
        
        $('a#drew-hover').mousemove(function(e) {
          $("div#drew-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Drew-->

      <!--Mike-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#mike-hover').hover(function(e) {
          $('div#mike-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#mike-box').hide();
        });
        
        $('a#mike-hover').mousemove(function(e) {
          $("div#mike-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Mike-->

      <!--Parvin-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#parvin-hover').hover(function(e) {
          $('div#parvin-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#parvin-box').hide();
        });
        
        $('a#parvin-hover').mousemove(function(e) {
          $("div#parvin-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Parvin-->

      <!--Natasa-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#natasa-hover').hover(function(e) {
          $('div#natasa-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#natasa-box').hide();
        });
        
        $('a#natasa-hover').mousemove(function(e) {
          $("div#natasa-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Natasa-->

      <!--Jenifer-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#jenifer-hover').hover(function(e) {
          $('div#jenifer-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#jenifer-box').hide();
        });
        
        $('a#jenifer-hover').mousemove(function(e) {
          $("div#jenifer-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Jenifer-->

      <!--Jc-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#jc-hover').hover(function(e) {
          $('div#jc-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#jc-box').hide();
        });
        
        $('a#jc-hover').mousemove(function(e) {
          $("div#jc-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Jc-->

      <!--Karen-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#karen-hover').hover(function(e) {
          $('div#karen-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#karen-box').hide();
        });
        
        $('a#karen-hover').mousemove(function(e) {
          $("div#karen-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Karen-->

      <!--Alex-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#alex-hover').hover(function(e) {
          $('div#alex-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#alex-box').hide();
        });
        
        $('a#alex-hover').mousemove(function(e) {
          $("div#alex-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Alex-->

      <!--Steve-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#steve-hover').hover(function(e) {
          $('div#steve-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#steve-box').hide();
        });
        
        $('a#steve-hover').mousemove(function(e) {
          $("div#steve-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Steve-->

      <!--Davey-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#davey-hover').hover(function(e) {
          $('div#davey-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#davey-box').hide();
        });
        
        $('a#davey-hover').mousemove(function(e) {
          $("div#davey-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Davey-->

      <!--Alison-->
      $(function() {
        var moveLeft = -250;
        var moveDown = 10;
        
        $('a#alison-hover').hover(function(e) {
          $('div#alison-box').show();
          //.css('top', e.pageY + moveDown)
          //.css('left', e.pageX + moveLeft)
          //.appendTo('body');
        }, function() {
          $('div#alison-box').hide();
        });
        
        $('a#alison-hover').mousemove(function(e) {
          $("div#alison-box").css('top', e.pageY + moveDown).css('left', e.pageX + moveLeft);
        });
        
      });
      <!--Alison-->

